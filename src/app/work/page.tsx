import { WorkList } from "@/components/work/WorkList";

export const metadata = {
  title: "работы — id.140dev",
  description: "Список проектов id.140dev"
};

export default function WorkPage() {
  return <WorkList />;
}
